const km = prompt( "Quanti km vuoi percorrere?" )
console.log(km)

const eta = prompt( "Quanti anni hai?" )
console.log(eta)

const ticket = parseFloat(0.21)

const sconto_1 = parseFloat

const sconto_2 = parseFloat

if(eta < 18) {
    sconto_1
}
else if(eta >= 18) {
    sconto_2 
}